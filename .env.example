# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Cantamus

# Database (local SQLite by default; set to Turso URL for production)
DATABASE_URL=file:./data/local.db
# DATABASE_AUTH_TOKEN=  # Required for Turso remote DB

# Admin auth (min 16 chars, used to sign session cookies)
ADMIN_SECRET=change-me-to-a-random-secret
